spring:
  datasource:
    url: ${MYSQL_URL}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    properties:
      hibernate:
        ddl-auto: create-drop

super_admin:
  email: ${SUPER_ADMIN_EMAIL}
